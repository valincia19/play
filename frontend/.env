# This file is intentionally minimal.
# Active configuration lives in:
#   .env.development  (npm run dev)
#   .env.production   (npm run build / Docker)
#
# See .env.example for all available variables.
