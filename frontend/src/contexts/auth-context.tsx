/* eslint-disable react-refresh/only-export-components -- Barrel re-export for backward compatibility */
// Re-export from hooks file to keep backward compatibility
export { AuthProvider, useAuth, type AuthContextType } from './auth-context.hooks'
